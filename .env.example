# Variables de entorno para MoodTune Frontend (Vite + React)
# Copia este archivo a .env y ajusta los valores segun tu entorno.

# URL base del API principal (Flask). En desarrollo local suele ser:
VITE_API_BASE_URL=http://localhost:8000

# Endpoint completo del servicio FER para deteccion facial.
# Ejemplo local (moodtune_fer):
VITE_FER_ENDPOINT_URL=http://localhost:8081/infer

# URL base del servicio RAG (busqueda/sugerencias de musica)
VITE_RAG_BASE_URL=http://localhost:8010

# URL base del servicio moodtune_music (creación de playlists)
VITE_MUSIC_BASE_URL=http://localhost:8020

# (Opcional) Token Bearer fijo para consumir el API si el backend lo requiere.
# Si no lo defines, el cliente usara el session_id guardado como Bearer.
#VITE_API_BEARER_TOKEN=

